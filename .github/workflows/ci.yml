name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Ajuste o setup abaixo para a stack real do projeto (Node, Python, etc.)
      - name: Setup runtime
        run: echo "TODO: configurar setup-node / setup-python conforme a stack"

      - name: Lint
        run: echo "TODO: rodar linter do projeto"

      - name: Unit tests
        run: echo "TODO: rodar tests/unit"

      - name: Integration tests
        run: echo "TODO: rodar tests/integration"
