name: Trivy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  trivy-fs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy filesystem scan (deps + IaC)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          severity: 'CRITICAL,HIGH'
          exit-code: '1'

  # Descomente se o projeto tiver Dockerfile
  # trivy-image:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Build image
  #       run: docker build -t app:ci .
  #     - name: Trivy image scan
  #       uses: aquasecurity/trivy-action@master
  #       with:
  #         image-ref: 'app:ci'
  #         severity: 'CRITICAL,HIGH'
  #         exit-code: '1'
